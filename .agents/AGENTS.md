
## Git Workflow
- **NEVER** run `git add`, `git commit`, or `git push` automatically on behalf of the user.
- The user prefers to run these commands manually.
- When work is complete, simply provide a 6-7 word git commit message (without writing 'feat:' or 'fix:' in front) and instruct the user to commit the changes themselves.

